---
query: "Run /audit on the new billing module"
expected_behavior: "The agent checks the Capability Matrix, looks for duplicated business rules, and flags any environment-sensitive assumptions (like missing optional schema). It does not prioritize code formatting over structural risks."
---
