name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Rebuild generated files
        run: |
          python .forge/forge.py build --all
          python .forge/forge.py commands --host all
          git diff --exit-code

      - name: Validate skills
        run: python .forge/validate_skill.py --all --strict .agents/skills

      - name: Validate library
        run: python .forge/validate_library.py --strict

      - name: Check documentation drift
        run: python .forge/doc_drift.py

      - name: Check release metadata
        run: python .forge/release_notes.py --check

      - name: Check eval structure
        run: |
          python .forge/eval_integrity.py
          python .forge/run_evals.py --all --check
          python .forge/orch_eval.py --check

      - name: Run Forge tests
        run: python .forge/tests/run.py

      - name: Scan source fingerprints
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: python .forge/fingerprint_scan.py --ref "$BASE_SHA" --head "$HEAD_SHA" --expect-changes
