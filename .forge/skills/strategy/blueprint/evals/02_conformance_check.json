{
  "skill": "resonance-strategy-blueprint",
  "query": "Run /blueprint check on this release candidate. The approved blueprint says billing decisions belong to the domain service and customer messages require durable delivery state. This change calculates eligibility in the browser, calls the payment provider directly, and marks the message sent when it is queued. Tests cover only the happy path.",
  "files": [],
  "expected_behavior": [
    "Treats the approved blueprint as the conformance baseline and cites the violated rules",
    "Identifies browser-owned eligibility, direct provider access, and queue-equals-delivery as three distinct boundary violations",
    "Traces the affected decision, state, side effect, failure path, and verification gap",
    "Ranks findings by consequence and blast radius rather than by style",
    "Returns a clear non-conforming or blocked verdict with the smallest safe correction path",
    "Does not rewrite the blueprint to make the release pass"
  ],
  "checks": [
    {"kind": "contains_any", "value": ["non-conform", "block", "reject"]},
    {"kind": "contains_any", "value": ["delivery", "delivered"]},
    {"kind": "contains_any", "value": ["browser", "client"]}
  ]
}
