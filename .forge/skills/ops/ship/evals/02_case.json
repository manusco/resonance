{
  "skill": "resonance-ops-ship",
  "query": "Run /ship but the build fails on the pre-flight check",
  "files": [],
  "expected_behavior": [
    "The agent aborts the procedure immediately and refuses to tag or push the release"
  ]
}
